pipeline {

  agent {
    label 'ios-node'
  }
  
  stages {
  
    stage('Configure') {
      steps {
        checkout scm
      }
    }

    stage('Build') {
      steps {
        sh 'xcodebuild -list'
      }
    }

  }

}
