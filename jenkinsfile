agent {
  label 'ios-node'
}

pipeline {
  
  stages {
  
    stage('Configure') {
      steps {
        checkout scm
      }
    }

    stage('Build') {
      steps {
        sh 'xcodebuild -list'
      }
    }

  }

}
